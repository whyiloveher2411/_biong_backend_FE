export const TEXT_CLIP_ENTER_DURATION_SEC = 0.5;
export const TEXT_CLIP_EXIT_DURATION_SEC = 0.5;
export const TEXT_CLIP_SLIDE_OFFSET_PERCENT = 40;

export type TextClipMotionStyle = {
    opacityFactor: number;
    translateXPercent: number;
    translateYPercent: number;
    scale: number;
};
